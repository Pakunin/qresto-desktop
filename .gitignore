# 1. Maven Build Artifacts and Output (Crucial)
# Excludes the main compiled output directory
/target/
# Excludes generated files within source folders (a safer way to handle this)
!**/src/main/**/
!**/src/test/**/

# Safely includes the Maven Wrapper JAR needed for others to build the project
!.mvn/wrapper/maven-wrapper.jar

---

# 2. IntelliJ IDEA Files (Focus on User/Local Config)
# Ignore the entire .idea folder contents except for specific, required shared files
.idea/
# EXCEPTION: Allow the files required for project setup and sharing
!/.idea/modules.xml
!/.idea/jarRepositories.xml
!/.idea/compiler.xml
!/.idea/vcs.xml # Important for shared VCS settings (like mapping git roots)
!/.idea/codeStyles/ # Recommended to share code style settings

# Explicitly ignore user-specific files that cause conflicts
.idea/workspace.xml
.idea/tasks.xml
.idea/libraries/
*.iws
*.iml
*.ipr

---

# 3. IDE Agnostic Exclusions
# These sections are included for developers who might use other IDEs.

### Eclipse ###
.apt_generated
.classpath
.factorypath
.project
.settings/
.springBeans
.sts4-cache

### NetBeans ###
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/
build/

### VS Code ###
.vscode/

---

# 4. OS Files
.DS_Store
